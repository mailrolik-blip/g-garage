# G-Garage mobile UX prototype

Static mobile-first SPA prototype for the future G-Garage store.

Open locally:

```powershell
python -m http.server 8087
```

Routes:

- `/prototype/`
- `/prototype/#/home`
- `/prototype/#/map`
- `/prototype/#/manager`

The current production homepage files in the repository root are not replaced by this prototype.